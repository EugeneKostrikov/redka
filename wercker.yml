box: google/nodejs
services:
  - redis
  - mongodb
build:
  steps:
    - npm-install
    - script:
      name: current environment
      code: |-
        node ./print-env.js
    - script:
      name: run tests
      code: |-
        npm test